/**
 * get the current clock cycles.
 * \return the number of passed clock cycles up to now.
 */
extern unsigned int time();
/**
 * to let program sleep for n miliseconds.
 * \param n the number of miliseconds you want to sleep.
 */
extern void sleep(int n);