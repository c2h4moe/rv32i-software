/**
 * get a random number in range [0..2^31).
 */
extern int rand();
/**
 * set a new seed for random number generator.
 * \param n the new seed you want to set to.
 */
extern void seed(int n);